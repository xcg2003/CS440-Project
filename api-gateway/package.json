{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "API Gateway — routes requests to book-search-service and book-library-service",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "tsc -p tsconfig.json && node dist/server.js"
  },
  "type": "module",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@fastify/static": "^9.1.3",
    "fastify": "^5.8.5"
  }
}
